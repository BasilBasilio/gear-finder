export interface InsertionData {
  id: string;
  instrumentType: string;
  model: string;
  description: string;
  rentalPrice: string;
  condition: string;
  location: string;
  deliveryMethod: string;
  imageUrl: string | undefined;
  notes: string;
}
